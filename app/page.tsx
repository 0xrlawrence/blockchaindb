import GrainientCover from "@/components/GrainientCover";

/**
 * Root route: a full-screen animated grainient cover. The "Enter" button
 * leads into the dashboard (which is the app itself, at /dashboard).
 */
export default function Home() {
  return <GrainientCover />;
}
